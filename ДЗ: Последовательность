using System;
using System.Security.Cryptography;

namespace proga
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int start = 5, step = 7;

            Console.Write("Введите любое число: ");
            int maxNumber = Convert.ToInt32(Console.ReadLine());

            for (int i = start; i <= maxNumber; i += step)
            {
                Console.WriteLine(i);
            }
        }
    }
}
